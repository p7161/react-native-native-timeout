# Native timeout example app

This directory contains a minimal React Native app that demonstrates how to use `react-native-native-timeout`. Install dependencies, then run the standard platform commands:

```bash
npm install
npm run ios
npm run android
```

The example renders two buttons – **Start 1s timeout** and **Cancel** – to exercise the native timers.
